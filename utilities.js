// ---------------------------------------------------------------------------------------
// name: DebugPrint(parMessage)
// ---------------------------------------------------------------------------------------
function debugPrint(parMessage) {

    if (window.console) {
        console.log(parMessage);
    }
}